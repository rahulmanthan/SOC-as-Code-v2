# SentinelGate — Environment Variables
# ======================================
# Copy this file to .env and fill in your values.
# .env is listed in .gitignore and must NEVER be committed.
# For GitHub Actions, add these as repository Secrets
# (Settings → Secrets and variables → Actions → New repository secret)

# Splunk REST API
SPLUNK_HOST=localhost
SPLUNK_PORT=8089
SPLUNK_USER=rahul_manthan
SPLUNK_PASSWORD=Rahulmanthan@2004

# Lab VM (Windows Server)
VM_HOST=192.168.10.136
VM_USER=Administrator
VM_PASSWORD=Password123

# Atomic Red Team paths on the VM (defaults are usually correct)
ATOMICS_PATH=C:\AtomicRedTeam\atomic-red-team\atomics
ATOMIC_MODULE=C:\Users\Administrator\Documents\WindowsPowerShell\Modules\Invoke-AtomicRedTeam\2.3.0\Invoke-AtomicRedTeam.psd1
